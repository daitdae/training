CREATE TABLE `test`.`USER_ACCESS_LOG` (
                                          `ID` INT(11) NOT NULL,
                                          `USER_ID` INT(11) NULL,
                                          `GAMEN_ID` VARCHAR(255) NULL,
                                          `START_TIME` DATETIME NULL,
                                          PRIMARY KEY (`ID`));

